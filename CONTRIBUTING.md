# Contributing to MathForge

MathForge 0.3 的目标是建立一个帮助学习者真正理解数学的开放学习核心。当前唯一正式贡献入口是 GitHub：

- Repository: <https://github.com/liqinglan0512/MathLearn>
- Issues: <https://github.com/liqinglan0512/MathLearn/issues>
- Pull Requests: <https://github.com/liqinglan0512/MathLearn/pulls>

网站内的普通投稿、附件上传和直接发布仍然关闭。请不要绕过 feature flags、路由守卫或数据层限制恢复这些入口。

## 1. 可以贡献什么

- 数学内容的解释改进；
- 数学错误、条件遗漏或证明漏洞修复；
- 服务于数学理解的可视化；
- 学习单元与练习之间的可靠关联；
- UI / UX 与无障碍改进；
- Bug 修复；
- 自动测试与质量门改进；
- 文档、翻译和来源说明。

当前不接受以恢复大型社区、用户上传、AI 解题、挑战赛、成长日记、排行榜、点赞、粉丝、信息流或私信为目标的改动。若提案需要引入外部服务、正式发布新学习单元或改变数学结论，请先开 Issue，由 Leo 明确决定范围。

## 2. 贡献流程

1. 先搜索现有 Issue 和 Pull Request，避免重复工作。
2. 对较大改动先开 Issue，说明要解决的问题、预期学习价值和范围边界。
3. Fork repository，并从最新主分支创建单一目的的分支。
4. 完成改动和对应验证后提交 Pull Request。
5. 根据 Code Review 修订；不要通过重写历史隐藏已经讨论过的数学或工程变化。

每个 Pull Request 必须回答：

### What

改了什么？列出受影响的页面、学习单元、数学结论、数据模型或工程文件。

### Why

为什么需要改？它如何帮助用户理解、看见、练习或改善数学内容？

### How verified

如何验证？请给出实际运行的命令、测试结果、浏览器检查范围，以及没有覆盖的部分。不要把“计划运行”写成“已经通过”。

## 3. 数学内容要求

数学内容贡献必须同时重视正确性、严谨性、可理解性和来源可靠性。涉及数学结论时，PR 还必须说明：

1. 修改了哪个定义、命题或结论；
2. 修改前后条件是否变化；
3. 证明是否变化，变化发生在哪一步；
4. 是否新增或移除了隐含假设；
5. 前置知识、后续知识或练习关联是否变化；
6. 参考来源是什么，并尽量提供可核验的版本、页码、章节或稳定链接；
7. 哪些内容是来源原文、现代解释、编辑推断或作者自己的补充。

不要仅凭计算样例、图像看起来正确或语言模型判断，就声称一个数学结论已经证明。反例、边界条件和退化情形应在适用时明确检查。

### AI 与自动化内容

- 不接受未经人工逐项核验的 AI 批量生成数学正文、证明、翻译或题目；
- LLM 的回答不能作为数学证明通过审核的依据；
- 若自动化工具实质性参与内容生成，请在 PR 中说明使用范围、人工核验方式和参考来源；
- 自动检查可以验证结构和公式语法，但不能替代人工数学审核。

### 合并不等于数学发布

Pull Request 合并只表示改动进入 Git 历史，不自动意味着内容已经：

- 通过数学审核；
- 达到 `math_reviewed`；
- 成为正式学习单元；
- 获得 `published` 或 `verified` 状态。

这些状态必须遵守项目既有的 Revision、Review、provenance 和人工发布边界。机器稿不得因为格式正确、页面可用或 PR 已合并而自动升级。

## 4. 可视化要求

可视化必须服务数学理解。PR 应说明：

- 用户正在观察什么数学对象；
- 哪些量可以改变；
- 改变参数时哪些关系或不变量保持不变；
- 图形是概念示意、命题专用构造，还是已经人工核验；
- 图形不能证明什么。

不要增加与数学无关的粒子、星空、霓虹或装饰性动画，也不要把通用示意图标成已经核验的命题构造。

## 5. 工程验证

安装依赖时使用已提交的 lockfile：

```bash
npm ci
```

提交前运行完整质量门：

```bash
npm run check
```

它当前覆盖 TypeScript、ESLint、Vitest、LaTeX、仓库卫生、产品 contract 和生产构建。涉及 UI、路由、交互或可视化的改动，还应记录实际浏览器 QA；自动测试不能代替全部交互检查。

依赖升级应保持范围小、说明安全原因并展示 lockfile 差异。不得使用破坏性的 `npm audit fix --force`。

## 6. 安全与隐私

- 不要提交密码、token、私钥、服务器地址凭据、本地浏览器 profile 或真实用户数据；
- 不要在公开 Issue 或 PR 中披露可直接利用的敏感漏洞细节；
- 当前私密漏洞报告渠道仍待 Leo 决定。在 `SECURITY.md` 或 GitHub 私密报告入口正式公布前，不要公开利用步骤；
- 当前账户、批注和学习记录主要是 localStorage 原型，不得描述为云端账户、生产级认证或可靠备份。

## 7. 许可证边界

仓库当前尚未建立由 Leo 确认的代码许可证和数学内容许可证。本文件不构成许可证、版权转让或对第三方材料的再许可。

在许可证明确前：

- 不要提交无权授权或来源不清的内容；
- 不要假定历史文本、翻译、图片或数学语料都能使用同一种许可证；
- 不要在文档中把“公开源码”自动等同于已经完成法律意义上的开源许可。

软件与数学内容的许可证选择、第三方语料权利核对以及贡献许可口径，均需要 Leo 另行决定。

## 8. PR 自检清单

- [ ] PR 已写明 What / Why / How verified。
- [ ] 改动范围单一，没有顺手恢复冻结功能。
- [ ] 数学变化已说明结论、条件、证明和来源。
- [ ] AI 或自动化辅助范围已经披露并经过人工核验。
- [ ] 新增内容没有伪造 `math_reviewed`、`published` 或 `verified` 状态。
- [ ] `npm run check` 已实际通过，或失败项已如实列出。
- [ ] UI 改动已记录浏览器 QA 范围。
- [ ] 没有提交凭据、本地数据、构建产物或调试垃圾。
